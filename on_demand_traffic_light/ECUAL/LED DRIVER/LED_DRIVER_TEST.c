/*
#include "../../UTILITIES/types.h"
#include "../../MCAL/DIO DRIVER/dio.h"
#include "LED.h"


uint8_t PIN_NUM = 0;
int main(void)
{

for (PIN_NUM = 0 ; PIN_NUM <8 ; PIN_NUM++)
{
	
LED_INIT(PIN_NUM,PORT_A);
LED_ON(PIN_NUM,PORT_A);
LED_OFF(PIN_NUM,PORT_A);

LED_INIT(PIN_NUM,PORT_B);
LED_ON(PIN_NUM,PORT_B);
LED_OFF(PIN_NUM,PORT_B);

LED_INIT(PIN_NUM,PORT_C);
LED_ON(PIN_NUM,PORT_C);
LED_OFF(PIN_NUM,PORT_C);

LED_INIT(PIN_NUM,PORT_D);
LED_ON(PIN_NUM,PORT_D);
LED_OFF(PIN_NUM,PORT_D);

}
}*/